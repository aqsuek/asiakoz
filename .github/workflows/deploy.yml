# NOTE:
# This repository is currently served via GitHub Pages + custom domain.
# FTP deployment is intentionally disabled to avoid false failing runs.
# If migration to external hosting is needed later, reintroduce FTP deploy separately.

name: Deploy site (disabled FTP)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment mode
        run: echo "GitHub Pages serves this repository directly; FTP deploy is disabled."
